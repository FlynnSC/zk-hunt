export const MAP_SIZE = 31;
export const BITS_PER_CHUNK = 253;

export const RESPONSE_PERIOD = 10; // In seconds

export enum TileType {
  PLAINS,
  JUNGLE,
}

export enum Direction {
  RIGHT,
  UP,
  LEFT,
  DOWN
}
